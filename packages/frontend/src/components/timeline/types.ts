// Re-export shared types for backward compatibility
export type { 
  FrontendTicket as Ticket,
  TimelineProps,
  DragState,
  TicketWithPosition,
  TimeMarker,
  TimelineViewport,
  TimelineEvents
} from '@wrm/types';